import React from 'react'
import './../App.css'

const Header = ({ cat }) => {
    return (
        <>
            <h1 className="Heading">Get all Latest News</h1>
            <header className="header">
            </header>
        </>
    )
}

export default Header
